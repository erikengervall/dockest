{
  "name": "@example/node-to-node",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "ts-node ./dockest.ts",
    "test:unit": "jest",
    "test:unit:watch": "jest --watch",
    "start:dev": "concurrently \"cd ./users && yarn start:dev\" \"cd ./orders && yarn start:dev\""
  },
  "dependencies": {
    "axios": "^0.19.0",
    "express": "^4.17.1"
  }
}
