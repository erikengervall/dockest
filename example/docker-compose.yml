version: '3'
services:
  postgres1sequelize:
    image: postgres:9.6.3
    volumes:
      - postgres1Volume:/var/lib/postgresql/data

  postgres2knex:
    image: postgres:9.6.3
    volumes:
      - postgres2Volume:/var/lib/postgresql/data

  kafka:
    image: docker.artifactory.klarna.net/kafka/kafka:0.11.0.0

volumes:
  postgres1Volume:
  postgres2Volume:
