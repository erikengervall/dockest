version: '3.8'

services:
  postgres:
    environment:
      POSTGRES_DB: nobueno
      POSTGRES_PASSWORD: is
      POSTGRES_USER: ramda
    image: postgres:9.6-alpine
    ports:
      - 5433:5432/tcp
