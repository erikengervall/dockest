{
  "name": "@examples/node-to-node--users",
  "version": "3.1.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node ./index",
    "docker:build": "docker build --no-cache -t users .",
    "docker:run": "docker container run --rm -p 1337:1337 -t users"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "express": "^4.17.1"
  }
}
