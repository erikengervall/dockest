{
  "name": "@examples/node-to-node--orders",
  "version": "3.0.0-beta.1",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node ./index",
    "docker:build": "docker build --no-cache -t orders .",
    "docker:run": "docker container run --rm -p 1338:1338 -t orders"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
