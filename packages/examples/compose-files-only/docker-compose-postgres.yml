version: '3.7'

services:
  composePostgres:
    image: 'postgres:9.6-alpine'
    ports:
      - '5433:5432'
    environment:
      POSTGRES_DB: nobueno
      POSTGRES_PASSWORD: is
      POSTGRES_USER: ramda
