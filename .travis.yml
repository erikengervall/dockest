language: node_js
node_js:
 - "node"
services:
  - docker
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash # yarn
  - export PATH=$HOME/.yarn/bin:$PATH # yarn
install:
  - yarn install-all